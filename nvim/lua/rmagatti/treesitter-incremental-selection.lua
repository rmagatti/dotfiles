return {
  enable = true,
  keymaps = {
    init_selection = "vv",       -- set to `false` to disable one of the mappings
    node_incremental = "v",
    scope_incremental = "grc",   -- not sure what this one does
    node_decremental = "V",
  },
}
