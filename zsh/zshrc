# History
HISTSIZE=10000
SAVEHIST=10000
HISTFILE=~/.zsh_history
setopt SHARE_HISTORY
setopt HIST_IGNORE_ALL_DUPS
setopt HIST_REDUCE_BLANKS

# Completions
autoload -Uz compinit
compinit

# PATH
export PATH="$HOME/.cargo/bin:$PATH"
export PATH="$HOME/.opencode/bin:$PATH"
export PATH="$HOME/.local/bin:$PATH"
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"
export PNPM_HOME="$HOME/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac

# Environment
export VISUAL=nvim
export EDITOR=nvim

# FZF
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
export FZF_DEFAULT_COMMAND="fd . $HOME"
export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"
export FZF_ALT_C_COMMAND="fd -t d . $HOME/Projects"

# NVM
export NVM_DIR="$HOME/.nvm"
[ -s "/opt/homebrew/opt/nvm/nvm.sh" ] && \. "/opt/homebrew/opt/nvm/nvm.sh"
[ -s "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm" ] && \. "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm"

# Bun completions
[ -s "$HOME/.bun/_bun" ] && source "$HOME/.bun/_bun"

# Zoxide
eval "$(zoxide init zsh --cmd cd)"

# Keybindings
bindkey -s ç 'cd $(find ~/Projects/ -maxdepth 2 -type d | fzf)\n'

# --- Aliases ---

# Rust tools
alias ls="eza"
alias l="eza -lh"
alias du="dust"
alias grep="rg"

# Git
alias gits="git status"
alias gitch="git checkout"
alias gitnb="git checkout -b"
alias gita="git add"
alias gitl="git log"
alias gitps="git push"
alias gitpl="git pull"
alias gitd="git diff"
alias gitb="git branch --sort=committerdate"
alias gitst="git stash"
alias gitstp="git stash pop"
alias gitcl="git clone"

# Editor
alias v="nvim"
alias vim="nvim"
alias vv="neovide --frame='transparent' --no-vsync&"

# Tools
alias gemini="GOOGLE_CLOUD_PROJECT=\"montra-development-env\" gemini"
alias oc="opencode"
alias ocw="OPENCODE_CONFIG_DIR=~/.config/opencode-work opencode"
alias ocp="OPENCODE_CONFIG_DIR=~/.config/opencode-personal opencode"

# --- Functions ---

function kc() {
  local ENV=${1:-"dev"}

  if [ "$ENV" = "dev" ]; then
    gcloud container clusters get-credentials montra-development-env-gke-cluster --project montra-development-env --region us-east1
  elif [ "$ENV" = "staging" ]; then
    gcloud container clusters get-credentials montra-staging-env-gke-cluster --project montra-staging-env --region us-east1
  elif [ "$ENV" = "production" ]; then
    gcloud container clusters get-credentials montra-production-env-gke-cluster --project montra-production-env --region us-east1
  else
    echo "Defaulting to development-env"
    gcloud container clusters get-credentials montra-development-env-gke-cluster --project montra-development-env --region us-east1
  fi
}

function gitc() {
  if [ -z "$1" ]; then
    echo WARN: No commit message was given. Running git commit without a message.
    git commit
  else
    branch="$(git symbolic-ref --short HEAD)"
    ticket="$(grep -Eo '(?i)sc[0-9]+|sc-[0-9]+|ENG.*-[0-9]+' <<< \"$branch\")"
    pair=($(grep -Eo '[[:alpha:]]+|[0-9]+' <<< \"$ticket\"))
    prefix="$pair[1]:u-$pair[2]"
    if [ -z "$ticket" ]; then
      git commit -m "$1"
    else
      git commit -m "$prefix: $1"
    fi
  fi
}

function gitf() {
  git for-each-ref --format='%(refname:short)' refs/heads/ | while read branch; do
    base=$(git merge-base --all $branch HEAD)
    for b in $base; do
      if [ "$b" = "$(git rev-parse HEAD^)" ]; then
        echo "Current branch might have branched from $branch (based on HEAD^ merge base)"
      fi
    done
  done
}

function gitcan() {
  git commit --amend --no-edit
}

function gitca() {
  git commit --amend
}

function gclone() {
  git clone git@github.com:Montra-Interactive/$1.git ~/Projects/Montra/$1 && cd ~/Projects/Montra/$1
}

function gitm() {
  git merge $(git branch --sort=-committerdate | fzf)
}

function gitmdev() {
  BRANCH_TO_MERGE=$(git symbolic-ref --short HEAD) && (git branch -D development 2>/dev/null || true) && git fetch origin development && git checkout development && git pull && git merge $BRANCH_TO_MERGE && git push && git checkout $BRANCH_TO_MERGE
}

function gitmDev() {
  BRANCH_TO_MERGE=$(git symbolic-ref --short HEAD) && git branch -D Development && git fetch origin Development && git checkout Development && git pull && git merge $BRANCH_TO_MERGE && git push && git checkout $BRANCH_TO_MERGE
}

function gitmalpha() {
  BRANCH_TO_MERGE=$(git symbolic-ref --short HEAD) && git checkout alpha && git pull && git merge $BRANCH_TO_MERGE && git push && git checkout $BRANCH_TO_MERGE
}

function gitchb() {
  git checkout $(git branch --sort=-committerdate | fzf)
}

function gitchr() {
  git fetch
  local branch=$(git branch --sort=-committerdate -r | fzf | sed 's/^[[:space:]]*//' | sed 's/[[:space:]]*$//')
  if git show-ref --verify --quiet "refs/heads/$branch"; then
    git checkout "$branch"
  else
    git fetch && git checkout --track "$branch"
  fi
}

function gitdc() {
  nvim $(git diff --name-only)
}

function gitpr() {
  local url
  url=$(gh pr view --json url -q .url 2>/dev/null)

  if [[ -n "$url" ]]; then
    echo "PR already exists:"
  else
    echo "Creating PR..."
    if ! gh pr create --fill; then
      echo "Failed to create PR" >&2
      return 1
    fi
    url=$(gh pr view --json url -q .url)
  fi

  echo "$url"
  echo -n "$url" | pbcopy
  echo "(copied to clipboard)"
}

function unstringify() {
  pbpaste | jq -r . | pbcopy
  echo "Unstringified copied to clipboard"
}

function gitsqa() {
  git reset $(git merge-base origin/master $(git rev-parse --abbrev-ref HEAD)) &&
  git add -A && git commit
}

function gitsqi() {
  git rebase -i $(git merge-base master $(git rev-parse --abbrev-ref HEAD))
}

# Brew helpers
function brr() {
  local uninst=$(brew leaves | eval "fzf ${FZF_DEFAULT_OPTS} -m --header='[brew:clean]'")

  if [[ $uninst ]]; then
    for prog in $(echo $uninst)
    do brew uninstall $prog
    done
  fi
}

function bru() {
  local upd=$(brew leaves | eval "fzf ${FZF_DEFAULT_OPTS} -m --header='[brew:update]'")

  if [[ $upd ]]; then
    for prog in $(echo $upd)
    do brew upgrade $prog
    done
  fi
}

function bri() {
  local inst=$(brew formulae | eval "fzf ${FZF_DEFAULT_OPTS} -m --header='[brew:install]'")

  if [[ $inst ]]; then
    for prog in $(echo $inst)
    do brew install $prog
    done
  fi
}

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/magatti/Projects/ThinkAlpha/nexus/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/magatti/Projects/ThinkAlpha/nexus/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/magatti/Projects/ThinkAlpha/nexus/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/magatti/Projects/ThinkAlpha/nexus/google-cloud-sdk/completion.zsh.inc'; fi
